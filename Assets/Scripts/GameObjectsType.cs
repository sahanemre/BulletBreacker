using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameObjectsType : MonoBehaviour
{
    public BulletT type;
    public enum BulletT { None, Bullet};
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
